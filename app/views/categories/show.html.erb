<div style="background: #3D9AD1; height: 200px; margin: 0 auto; width:600px;">
	<img src="/assets/fans-losing-it.jpeg" style="opacity: 0.15; background: rgb(61,154,209);">
	<h1 style="position: absolute; font-size:300%; color: #ffffff; margin-top: -115px; margin-left: 125px; text-align:center;">TheBandwagn</h1>
</div>
<div style="margin: 0 auto; width:600px;">

	<% if user_signed_in? %>
		<%= link_to 'Create a new email', new_category_path %><br>
		<% if @category == @category_for_email %>
			This is the email that will be sent.
		<% else %>
			This is NOT the email that will be sent.
		<% end %>
	<% end %>
	<br>
	<div>
		<% if @blurbs.any? %>
			<h2 style="text-align:center;"><%= @category.title %></h2>
			<div style="text-align:center;"><%= @category.created_at.strftime("%B %e, %Y").to_s %></div><br>
			<div style="text-align:center;">
				<%= link_to "<< Signup", root_path %> | <%= link_to "<< Archive", categories_path %>
			</div>
			<h3>STAT OF THE DAY</h3>
			<p><%= raw(@category.description) %></p>
			<p>
				<% if user_signed_in? %>
					<%= form_for(@blurb) do |f| %>
						<%= f.text_field :name, placeholder: "Enter a headline..." %>
						<%= f.cktext_area :content, placeholder: "Compose new blurb..." %>
						<%= f.hidden_field :category_id, value: @category.id %>
						<%= f.submit "Post", class: "btn btn-large btn-primary" %>
					<% end %>
				<% end %>
			</p>
			<ol class="blurbs">
				<%= render @blurbs %>
			</ol>

		<% else %>
			<%= "Not much to mention right now.  This sport will probs become relevant in a few months." %>
		<% end %>
	</div>
</div></br>



