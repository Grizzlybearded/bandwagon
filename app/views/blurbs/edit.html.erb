<div class="row">
  <div class="span6 offset3">
    <%= form_for(@blurb) do |f| %>
    
      <%= f.label :name %>
      <%= f.text_field :name %>  
      
      <%= f.label :content %>
      <%= f.cktext_area :content %>

      <%= f.submit "Save changes", class: "btn btn-large btn-primary" %>

    <% end %>
  </div>
</div>